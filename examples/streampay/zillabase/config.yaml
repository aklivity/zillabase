# config

admin:
  tag: develop-SNAPSHOT
zilla:
  tag: develop-SNAPSHOT
keycloak:
  realm: zillabase
  client:
    client-id: streampay
    redirects:
      - http://localhost:8000/*
      - https://oauth.pstmn.io/v1/callback
